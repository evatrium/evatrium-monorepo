import { describe, it, expect } from "vitest";
import { deepMergeSimple } from '~/deepMergeSimple';

describe('deepMergeSimple', () => {
	it('returns expected output', () => {
		expect(true).toBe(true);
	})
})
